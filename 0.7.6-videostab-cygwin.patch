--- origsrc/mlt-0.7.6/src/modules/videostab/stab/estimate.c	2011-10-31 12:26:04.000000000 -0500
+++ src/mlt-0.7.6/src/modules/videostab/stab/estimate.c	2011-12-19 04:03:32.020723300 -0600
@@ -18,7 +18,7 @@
 #include <stdlib.h>
 #include <math.h>
 #include <string.h>
-#if !defined(__DARWIN__)
+#if !defined(__DARWIN__) && !defined(__CYGWIN__)
 #include <values.h>
 #endif
 
--- origsrc/mlt-0.7.6/src/modules/videostab/stab/klt/base.h	2011-10-31 12:26:04.000000000 -0500
+++ src/mlt-0.7.6/src/modules/videostab/stab/klt/base.h	2011-12-19 03:56:14.660707700 -0600
@@ -13,6 +13,7 @@
 #define schar signed char
 #endif
 
+#ifndef __CYGWIN__
 #ifndef uint
 #define uint unsigned int
 #endif
@@ -24,12 +25,13 @@
 #ifndef ulong
 #define ulong unsigned long
 #endif
-
+#endif
+
 #ifndef max
-#define max(a,b)	((a) > (b) ? (a) : (b))
-#endif
+#define max(a,b)	((a) > (b) ? (a) : (b))
+#endif
 #ifndef min
-#define min(a,b)	((a) < (b) ? (a) : (b))
+#define min(a,b)	((a) < (b) ? (a) : (b))
 #endif
 #define max3(a,b,c)	((a) > (b) ? max((a),(c)) : max((b),(c)))
 #define min3(a,b,c)	((a) < (b) ? min((a),(c)) : min((b),(c)))
